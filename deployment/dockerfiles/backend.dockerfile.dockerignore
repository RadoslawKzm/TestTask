# This file takes root of app as starting point.
# Above behavior depends where context is set inside docker-compose build context.
# https://stackoverflow.com/questions/40904409/how-to-specify-different-dockerignore-files-for-different-builds-in-the-same-pr
# https://stackoverflow.com/questions/28097064/dockerignore-ignore-everything-except-a-file-and-the-dockerfile

# Ignore everything
*

# Allow files and directories
!/backend
!/deployment
